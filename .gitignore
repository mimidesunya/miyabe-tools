
# Production data (deployed to remote)
/data/*
!/data/config.example.json
!/data/municipalities/
!/data/municipalities/**
deploy.json
secret.json

# Local working data (scraping outputs, intermediate files)
/work/

# KMZ map files
/kmzs/

# Runtime
/logs/
/.venv/
/.venv*/
/.claude/
__pycache__/
*.py[cod]
node_modules/
docker/mcp/dist/

# Temporary files
tmp_*.html
# AIエージェント用ローカルガイド（git管理外）
AGENTS.md
CLAUDE.md

# OCR ツール（NDLOCR-Lite）。モデルを含めて 200MB あるので git には入れない。
# サーバーへは deploy/sync_ocr_tool.py で直接同期する。
/vendor/
