playwright==1.50.0
requests>=2.31.0
beautifulsoup4>=4.12.0
# AES 暗号化 PDF の展開に cryptography が要る。無いと
# 「cryptography>=3.1 is required for AES algorithm」で取得が失敗する。
pypdf[crypto]>=4.0.0
sudachipy>=0.6
sudachidict_core
celery[redis]>=5.4,<6
psycopg[binary]>=3.2,<4
# 文字情報のない PDF を OCR する（NDLOCR-Lite）。GPU は要らない。
# モデルは vendor/ndlocr-lite に同梱のものを使うので、実行時の取得は無い。
onnxruntime>=1.20
opencv-python-headless>=4.10
pypdfium2>=4.30
numpy>=2.0
lxml>=5.0
reportlab>=4.0
PyYAML>=6.0
pillow>=10.0
tqdm>=4.66
networkx>=3.0
yaspin>=3.0
matplotlib>=3.8
pandas>=2.0
